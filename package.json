{
  "name": "rhracers",
  "version": "1.0.0",
  "description": "Rolling Hills Racers - A 2D swimming race game",
  "main": "src/main.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "mkdir -p dist && cp -r index.html src dist/",
    "preview": "wrangler dev --local",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "phaser": "^3.85.2",
    "@cloudflare/kv-asset-handler": "^0.3.4"
  },
  "devDependencies": {
    "wrangler": "^3.114.9"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}